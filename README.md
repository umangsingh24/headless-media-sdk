# headless-media-sdk
The core requirement is to build a small Headless Media SDK ecosystem around the Pexels API, with strict separation between data/SDK, platform wrappers, UI components, and the final application.
